import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ca.pswdirect.worker",
  appName: "PSW Direct Worker",
  webDir: "../../dist-worker",
};

export default config;
