RewriteEngine On
RewriteBase /

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirect psadirect.ca → pswdirect.ca (301 permanent, preserve path)
RewriteCond %{HTTP_HOST} ^(www\.)?psadirect\.ca$ [NC]
RewriteRule ^(.*)$ https://pswdirect.ca/$1 [L,R=301]

# Redirect www.pswdirect.ca → pswdirect.ca (canonical host)
RewriteCond %{HTTP_HOST} ^www\.pswdirect\.ca$ [NC]
RewriteRule ^(.*)$ https://pswdirect.ca/$1 [L,R=301]

# Canonicalize short /{lang}-psw-{city} → /{lang}-speaking-psw-{city} (301 permanent)
RewriteRule ^(english|french|punjabi|hindi|urdu|tamil|gujarati|mandarin|cantonese|tagalog|spanish|portuguese|italian|polish|ukrainian|russian|arabic|farsi|korean|vietnamese|bengali|telugu|marathi|somali|amharic|swahili|greek|turkish)-psw-([a-z0-9-]+)/?$ /$1-speaking-psw-$2 [L,R=301]

# Force public SEO prefixes to SPA shell, even if stale files still exist at the edge
RewriteRule ^((home-care|ontario|private-home-care|in-home-care|senior-care|psw)-.*|personal-support-workers-ontario|about|faq)/?$ /index.html [L]

# If the request is for an existing file, serve it directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# SPA fallback - serve index.html for all routes
RewriteRule ^ index.html [L]
